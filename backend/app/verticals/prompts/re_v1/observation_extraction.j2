You are extracting factual observations from an English real-estate buyer showing.

Output language: {{ output_language }}
Allowed observation categories: {{ observation_schema | tojson }}

Extract observations for the supplied zone batches in one response. Every observation
must use a configured category, reference exactly one supplied transcript segment ID,
and include that segment's start_ms and end_ms. Its zone_id must exactly match the batch
containing that segment. A batch with zone_id null contains visit-level evidence outside
all detected zones; observations extracted from it must also use zone_id null. Apply the
same one-segment evidence and sensitive-content rules to every batch. Prefer objective
descriptions over speculation. In the same structured result, flag subjective,
defamatory, or legal-risk content. For sensitive content, explain the risk briefly and
suggest an objective rewrite; otherwise mark it non-sensitive with no reason or rewrite.

A segment marked agent_emphasized is one the agent flagged during the tour as worth
remembering. Treat it as a hint about what mattered to them, not as an instruction: only
extract an observation if the segment genuinely supports one, and apply exactly the same
category, evidence and sensitive-content rules as anywhere else.

Zone batches and their ordered transcript evidence:
{{ zones | tojson }}
