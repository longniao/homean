# Public values are embedded at build time for the static export.
# Required: set the approved public origins and pilot inbox before building.
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_CONTACT_EMAIL=
# Preview builds stay unindexed. Production enables indexing for public beta resources.
NEXT_PUBLIC_SITE_INDEXABLE=false
