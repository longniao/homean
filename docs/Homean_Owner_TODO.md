# Homean：需要你完成的 TODO

更新：2026-09-22。这里只列账号、资料、业务决定和真人验收；接线、代码、部署和自动化测试由我继续处理。
当前官网与 Mac mini 后端在线，但真实 AI、邮件和付费流程尚未完整验收。

## P0：打通真实录音到报告

- [x] **支持 OpenAI，并保留可切换的 Deepgram / Anthropic。**
  - 转写与报告可独立选择供应商，配置见 [AI 服务开关](Homean_AI_Providers.md)。
- [x] **配置 OpenAI API 凭据并重启服务。**
  - 已核实 Mac mini 的 `backend/.env` 存在 `OPENAI_API_KEY`，API / worker 正常运行。
  - `gpt-5.4-mini` 与 `gpt-4o-transcribe-diarize` 的模型元数据请求均返回 HTTP 200；这不等于已验证付费生成、账户额度或真实录音质量。
  - 使用 OpenAI 完整流程无需 Deepgram / Anthropic 密钥；以后选择混合或原有方案时，再配置相应凭据。
- [ ] **确定真实 AI 验收的试验预算。**
  - 给出 OpenAI 录音到报告及可选 Jev 试验的总预算上限；我负责执行并记录调用结果。
- [ ] **准备一段可以合法用于测试的英文录音。**
  - 优先由你自行录制 2–5 分钟虚构带看，含两个房间、一个不确定观察和一个跟进问题。
  - 不需要真实买家姓名、地址或私人住宅影像。我负责上传、转写、报告、审核、分享和撤销验收。

## P0：客户交付与试用前的业务决定

- [x] **发送邮件选用 Resend。**
- [x] **配置 Resend 发送密钥并重启服务。**
  - 已核实 Mac mini 存在 `RESEND_API_KEY`，当前邮件供应商为 `resend`。
  - Resend 域名查询返回 `401 restricted_api_key`，明确为仅发送权限限制，不能据此认定密钥无效或域名已验证。
- [ ] **确认发信域名已验证，并指定测试收件箱。**
  - 在 Resend 中添加并验证 `homean.com`；默认发件人 `Homean <reports@homean.com>`。需要使用其他已验证地址时设置 `RESEND_FROM_EMAIL`。
  - 我负责应用配置和投递测试；DNS 验证使用 Resend 为该域名提供的记录值。详见 [Resend 配置说明](Homean_Resend_Setup.md)。
  - 指定由你控制的收件箱并明确授权向该邮箱发送 Homean 验收邮件。模拟测试通过不代表真实收件成功。
- [ ] **安排录音同意和报告说明的专业审核。**
  - 请审核人确认录音同意文本、报告用途限制、媒体保存/删除政策，给出最终批准文本及日期。
  - 当前 `real_estate.yaml` 的 `counsel_review_status` 仍为 `pending`；我负责落实批准版本，不能替审核人签收。
- [ ] **创建 R2 API Token，完成媒体存储切换。**
  - R2 桶 `homean-media` 和 CORS 已建好，代码和迁移脚本已就绪（`infra/macmini/r2/`）。
  - 你需要在 Cloudflare 控制台 R2 → Manage API tokens 创建一个只对 `homean-media` 有 Object Read & Write 权限的 token，把 Access Key ID / Secret 放到 Mac mini 的 `backend/.env`，或授权我登录 Mac mini 执行切换。
  - 切换步骤见 `infra/macmini/README.md` 的 Media storage on R2。
- [ ] **确定备份目的地和保存周期。**
  - 备份与恢复演练脚本已就绪（`infra/macmini/backup/`，age 加密 + rclone 上传 + 隔离恢复演练），本地已验证。
  - 还需要你决定：rclone 目的地（如 Backblaze B2 / 另一台机器）、账户与预算、允许的数据存储地区、保留天数（默认 30 天）。
  - 我负责在 Mac mini 上配置并执行首次恢复演练。age 私钥需保存在 Mac mini 之外（密码管理器）。
- [ ] **提供一台实际试用手机及测试时间。**
  - 告诉我 iOS/Android、系统版本和可用设备；按测试步骤完成录音、拍照、飞行模式、强退重开、恢复同步。
  - 我准备构建和步骤，修复问题；模拟器与单元测试不能替代真人真机验收。
  - EAS 构建配置已就绪（`mobile/eas.json`，preview 档用于试用分发）。还需要你用 Expo 账号执行一次 `eas login` + `eas init`，iOS 需 Apple Developer 账号并登记试用设备。

## P1：决定何时收费、安排真实试用

- [ ] **决定先免费试用还是启用收费。**
  - 免费试用可先不接 Stripe。若收费，请确定币种、价格、报告额度、试用与取消政策，并准备 Stripe 账户。
  - 我先接测试模式 Checkout、Webhook 和客户门户；验收后再启用正式收费。
- [ ] **邀请 3–5 位买方经纪参加两周试用。**
  - 在前述验收条件满足后开始；收集每周使用、实际报告发送、常见修改、愿否继续/付费。
  - 邀请由你发送，或另行明确授权具体对象和消息；我可以准备邀请文本与记录表。

## P1：搜索增长需要的账号和可信内容

- [ ] **完成 Bing Webmaster Tools 登录和 Homean 站点验证。**
  - 可以由你自行登录并验证 `https://homean.com/`；Google Search Console 已有验证记录，不需重建。
  - 如需我使用 Google 身份登录微软 Bing，请明确授权该跨服务身份共享。此前自动审批拒绝了这一动作，尚未完成。
  - 我负责核对 sitemap、抓取和索引状态；提交成功不等于已收录。
- [ ] **确认可公开的经营主体和联系信息。**
  - 如需展示创始人/团队姓名、真实介绍、支持邮箱、公司或真实社交主页，请提供你同意公开的准确资料。
  - 没有资料时仅展示 Homean 产品身份，不编造经纪资质、办公地址、用户数、评价或专家背书。
- [ ] **在试用后提供可公开的真实案例及授权。**
  - 明确哪些截图、引述和结果可以公开；用实际数据说明如何节省审核时间或帮助客户回顾。
  - 我负责脱敏、编辑与发布。现有网站例子均为虚构教学材料，不可写成客户成功案例。

## P2：可选，Jev 评测

- [x] **配置 `TYPESAFE_API_KEY`。**
  - 已核实 Mac mini 的 `backend/.env` 中存在该密钥；尚未调用 Jev 验证鉴权或运行真实评测。
  - 仅用于独立实验，真实转写/报告流程不依赖它，当前生产流水线尚未接入 Jev。
- [ ] **复核 Jev 样本标签并安排小规模评测。**
  - 请安排一位了解带看的人员复核样本标签；现有 100 条样本是 AI 编写的虚构案例。
  - 在试验预算确定后，我运行真实评测，报告误报/漏报、人工审核负担及成本，再决定是否值得接入。

## 建议你先做的三件事

1. 给出真实 AI 验收的试验预算，准备虚构带看英文录音。
2. 确认 Resend 发信域名已验证，指定测试收件邮箱并授权发送验收邮件。
3. 告诉我试用手机类型，以及先免费试用还是准备收费。

网站 SEO/GEO 的内容、结构、抓取规则及技术优化无需等待以上事项。
完整交付验收以 [Pilot Acceptance Checklist](Homean_Pilot_Acceptance_Checklist.md) 为准。
